spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/attus_prazos
    username: attus
    password: attus123
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

logging:
  level:
    root: WARN
    ai.attus.prazos: INFO
